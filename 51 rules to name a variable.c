#include <stdio.h>
int main()
{
  printf("registration number :- RA2211042010033\n");
  char name = Tanvi; //variables are case sensitive 
  char NAME = tanvi; 
  /* variables are case sensitive so both nvalues will be stored in different memory location */
  int final_price = 100;  // only underscore is allowed in variable naming 
  int age = 20; // naming the variable accpording to its value helps while wriing huge codes 
  
  
 return 0;
}
