#include <stdio.h>
main()
{
  int a, b, c, big ;
  scanf("%d %d %d , &a, &b, &c);
        printf("a = %4d, b = %4d, c = %4d\n" , a, b, c);
        big = a;
        if(b > big)
        big = b 
        if (c > big)
        big = c ;
        printf("the largest number is %4d\n", big);
        }
